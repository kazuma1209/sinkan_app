<div class=user>

  <img src="<%= @user.image %>">
  <h2><%= @user.name %></h2>

</div>
<div class="col-md-8">
    <% if @user.microposts.any? %>
      <h3>投稿一覧 (<%= @user.microposts.count %>)</h3>
      <ol class="microposts">
        <%= render @microposts %>
      </ol>

    <% end %>
</div>
